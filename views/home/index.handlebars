<!DOCTYPE html>

<html lang="de" dir="ltr">

<head>
	<meta charset="UTF-8">
	<link rel="stylesheet" href="/static/css/style.css">
	<title>
		Hausaufgabenverwaltung
	</title>
</head>

<body>
	{{#if user}}
	<header>
		<h1>{{user.name}}, {{user.firstname}}</h1>
		<h2>{{currentRegister}}</h2>
	</header>
	<nav>
		<ul>
			<li>Logout</li>
			{{#each register}}
			<li><a href="{{this.link}}">{{this.title}}</a></li>
			{{/each}}
		</ul>
	</nav>
	<main></main>
	{{else}}
	<header>
		<h1> Willkommen bei der Hausaufgabenverwaltung! </h1>
	</header>
	<nav>
		<ul>
			<li><a href="/login">Login</a></li>
		</ul>
	</nav>
	<main>
		<h1>Willkommen zur Hausaufgabenverwaltung.</h1>
		<p>Melden sie sich <a class="text-link" href="/login">hier</a> an, um auf das Program zugreifen zu können.</p>
	</main>
	{{/if}}
</body>

</html>